import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Preliminary Trailer Build Specification | Alkota UK',
  description: 'Preliminary technical specification generated by the Alkota UK bespoke trailer configurator.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function DynamicBuildLayout({ children }: { children: React.ReactNode }) {
  return children;
}
